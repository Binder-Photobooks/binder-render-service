	{
  "name": "binder-render-service",
  "version": "1.0.0",
  "description": "Server-side PDF rendering worker for Binder. Drives the live site's own, already-correct rendering code via a real headless Chrome instance, so an order's print PDF can be generated reliably in the background instead of depending on an admin's browser tab staying open.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "puppeteer": "^24.15.0"
  }
}
